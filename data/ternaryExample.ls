main {
    var int x = 5;
    var int y = 7;
    var int result;
    result = x > y ? x : y;
    6 < 8 ? for ( x = 0; x < 12; x ++){
        print(x);
    }: for( y = 12; y > 0; y --){
        print(y);
    };
}