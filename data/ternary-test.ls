main {
    var int a = 2;
    var int b = 3;
    var int x;
    x = a > b ? a : b;
    4 < 5 ? for ( a = 0; a < 10; a ++){
        print(a);
    }: for( b = 10; b > 0; b --){
        print(b);
    };
}